---
id: TRL0017
title: Code review standard and review shortcut
status: completed
milestone: Beta
completed_on: 2026-06-25
priority: Medium
effort: 3
depends_on: []
summary: Add a repository code-review standard — a review shortcut that grounds in the repo's conventions and emits the canonical JSON findings format (file/line/severity/suggestion), scaffolded by init and graduating to an MCP prompt.
---

# TRL0017 — Code review standard and review shortcut

## Completed
Shipped `docs/playbooks/code-review.md` — a tech-agnostic code-review shortcut:
ground in the repo's own conventions (AGENTS/SPEC), run a disciplined multi-pass
review, and emit the canonical JSON findings array (`file`, `line`, `severity`
blocker/warning/nit, `suggestion`; discussion first, array last, `[]` when clean).
Ports the Taproot review convention into the Trellis recipe, minus Taproot's
stack-specific checks. Linked from AGENTS.md.

No work outstanding here: scaffolding this playbook into new repos is owned by
`trellis init` (TRL0003), and graduating it to an MCP prompt is owned by TRL0006.

## Scope
- A review shortcut (`docs/playbooks/code-review.md`): ground in the repo's
  conventions (AGENTS/SPEC) -> disciplined multi-pass review -> emit the
  canonical JSON findings array.
- Keep the *what-to-check* deferring to each repo's own conventions, so the
  standard is tech-agnostic; the process and output format are universal.

## Notes
- init wiring (TRL0003) and the MCP-prompt graduation (TRL0006) are tracked by
  those tasks, not here.

## Risks
- Drifting from the PR-draft shortcut's conventions; keep both playbooks consistent.
