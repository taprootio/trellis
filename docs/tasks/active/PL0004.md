---
id: PL0004
title: Build the MCP server (operations as tools)
status: active
milestone: Alpha
priority: High
effort: 8
depends_on: [PL0002]
summary: Expose the backlog operations over MCP so any MCP-aware tool can list, read, create, move, validate, and regenerate tasks in any repo.
---

# PL0004 — Build the MCP server (operations as tools)

## Scope
- Tools: `list_tasks`, `get_task`, `next_id`, `create_task`, `move_task`
  (active→completed/removed), `validate`, `regenerate`.
- Operate on the repo the client points at; reuse the PL0002 core for all logic.
- Return structured results (the `backlog.json` shape) for client rendering.

## Notes
- This is the vendor-neutral core — the open protocol is what makes the formula
  portable across Claude, Cursor, Windsurf, Codex, etc.

## Risks
- Write operations racing the generator. Make every mutating tool regenerate and
  validate before returning.
