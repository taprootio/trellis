# Completed (generated index — not a task)

This mimics a Trellis generator artifact. It sits in `completed/` beside the nested
`completed/tasks/` dir; the `trellis` profile reads the nested dir, so this is never
selected as a task.
